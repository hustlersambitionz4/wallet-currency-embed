{
  "scripts": {
    "dev": "npx tailwindcss -i ./src/base.css -o ./src/style.css --watch",
    "build": "rm -rf ./docs/ && cp -R ./src/. ./docs && npx tailwindcss -i ./src/base.css -o ./docs/style.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "3.3.3"
  }
}